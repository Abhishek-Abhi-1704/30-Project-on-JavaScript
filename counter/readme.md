# Counter App

## Features

* Increment counter value
* Decrement counter value
* Reset counter to zero
* Dynamic UI updates

## Technologies Used

* HTML5
* CSS3
* JavaScript (ES6)

## How It Works

* The counter starts at `0`.
* Clicking **Increment** increases the counter by `1`.
* Clicking **Decrement** decreases the counter by `1`.
* Clicking **Reset** sets the counter back to `0`.
* JavaScript updates the displayed value in real time using DOM manipulation.
